<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>OKD – TechBlog about OpenShift/Ansible/Satellite and much more</title><link>https://blog.stderr.at/tags/okd/</link><description>TechBlog about OpenShift/Ansible/Satellite and much more</description><generator>Hugo 0.164.0</generator><language>en-us</language><copyright>Toni Schmidbauer &amp; Thomas Jungbauer</copyright><lastBuildDate>Wed, 10 Jun 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://blog.stderr.at/tags/okd/index.xml" rel="self" type="application/rss+xml"/><item><title>RBAC Overview — an OpenShift Console plugin</title><link>https://blog.stderr.at/openshift-platform/security/rbac/2026-06-10-rbac-overview-console-plugin/</link><guid isPermaLink="true">https://blog.stderr.at/openshift-platform/security/rbac/2026-06-10-rbac-overview-console-plugin/</guid><pubDate>Wed, 10 Jun 2026 00:00:00 +0000</pubDate><dc:creator>Articles by Thomas Jungbauer</dc:creator><category>OpenShift</category><category>Security</category><category>Day-2</category><description>Install and use the RBAC Overview dynamic console plugin to inspect cluster admins, namespace access, subjects, who-can queries, and SCC authorisations from the OpenShift web console.</description><content:encoded><![CDATA[<div class="paragraph">
<p>Answering the question <strong>&#34;who can do what on this cluster?&#34;</strong> usually means juggling oc commands, YAML dumps, and several console pages. RoleBindings live in namespaces, ClusterRoleBindings are cluster-scoped, OpenShift adds Users and Groups on top of Kubernetes subjects, and SecurityContextConstraints have their own authorisation model. For a quick audit before a change window, that is a lot of clicking and I always struggled to find a quick and reliable way to get and overview. For example, I always wanted to see immediately which users have cluster-admin rights or which service accounts are on the privileged SCC.</p>
</div>
<div class="paragraph">
<p>The <strong>RBAC Overview</strong> is a dynamic <strong>OpenShift Console plugin</strong> that pulls these views into one place. This article walks through what it does, how to install or test it, and how I use the tabs in practice. This open source plugin is available on <a href="https://github.com/tjungbauer/openshift-console-rbac-overview" rel="noopener" target="_blank">GitHub</a></p>
</div>
<div class="sect1">
<h2 id="_why_another_rbac_view">Why another RBAC view?</h2>
<div class="sectionbody">
<div class="paragraph">
<p>The console already lists RoleBindings, ClusterRoleBindings, Users, and Groups. That is sufficient when you know exactly which resource you need. It is less helpful when you start from a question:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>Who has cluster-admin outside the usual system groups?</p>
</li>
<li>
<p>What can user alice do in the namespace openshift-gitops?</p>
</li>
<li>
<p>Which groups may get pods in the namespace default?</p>
</li>
<li>
<p>Which service accounts are on the privileged SCC?</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>The RBAC Overview plugins tries to fix these questions. Each tab uses the Kubernetes and OpenShift authorization APIs (<code>SelfSubjectReview</code>, <code>ResourceAccessReview</code>, <code>SubjectAccessReview</code>, and list permissions) rather than re-implementing policy evaluation in the browser.</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_what_you_get">What you get</h2>
<div class="sectionbody">
<div class="paragraph">
<p>Navigation: The plugin hooks into the <strong>Administrator perspective → User Management → RBAC Overview</strong> (entry appears above <strong>Users</strong>).</p>
</div>
<div class="paragraph">
<p>As direct URL you can use: <code>&lt;cluster-url&gt;/rbac-overview</code>.</p>
</div>
<div class="paragraph">
<p>The following tabs are available:</p>
</div>
<table class="tableblock frame-all grid-all stretch">
<colgroup>
<col style="width: 25%;"/>
<col style="width: 75%;"/>
</colgroup>
<thead>
<tr>
<th class="tableblock halign-left valign-top">Tab</th>
<th class="tableblock halign-left valign-top">Question it answers</th>
</tr>
</thead>
<tbody>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>Who can</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">Who may perform a certain action or whether one subject is allowed to do certain actions. It basically mirrors the <code>oc adm policy who-can</code> command and presents the results in a more user-friendly way.
The tab also includes a <strong>Show my access</strong> button for the logged-in user, which shows what the logged in user can do on the cluster.</p></td>
</tr>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>Cluster admins</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">Cluster-wide and namespace-scoped bindings to sensitive roles (<code>cluster-admin</code>, <code>admin</code>, or roles you configure). This helps to see which users have elevated rights and which namespaces are affected.</p></td>
</tr>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>Namespace access</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">All bindings that affect one namespace — local RoleBindings and applicable ClusterRoleBindings.</p></td>
</tr>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>Subjects</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">Pick a user, group, or service account and inspect its bindings. This helps to inspect a certain subject and see which roles are bound to it.</p></td>
</tr>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>Role access</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">Pick a ClusterRole and list every ClusterRoleBinding and RoleBinding that references it — the inverse of the Subjects tab when you start from a role name instead of a user or group.</p></td>
</tr>
<tr>
<td class="tableblock halign-left valign-top"><p class="tableblock"><strong>SCC access</strong></p></td>
<td class="tableblock halign-left valign-top"><p class="tableblock">All SCCs with key fields, direct <code>users</code> / <code>groups</code> / <code>serviceAccounts</code> on each SCC, and RBAC grants to <code>use securitycontextconstraints</code>. This helps to see which users, groups, and service accounts are on which SCCs and which RBAC grants are in place.</p></td>
</tr>
</tbody>
</table>
<hr/>
</div>
</div>
<div class="sect1">
<h2 id="_architecture_in_brief">Architecture in brief</h2>
<div class="sectionbody">
<div class="paragraph">
<p>The plugin follows the usual dynamic plugin pattern:</p>
</div>
<div class="olist arabic">
<ol class="arabic">
<li>
<p>Webpack builds static assets (<code>plugin-manifest.json</code>, JavaScript bundles, locales).</p>
</li>
<li>
<p>A container image serves those files over <strong>HTTPS on port 9443</strong> (required by the <code>ConsolePlugin</code> API).</p>
</li>
<li>
<p>Helm installs Deployment, Service, ConsolePlugin, ConfigMap (<code>plugin-config.json</code>), and optionally a Job that enables the plugin on the console. (Plugins must be enabled by the console operator.)</p>
</li>
</ol>
</div>
<div class="paragraph">
<p>The runtime image uses <a href="https://hummingbird-project.io/" rel="noopener" target="_blank">Project Hummingbird</a> (<code>registry.access.redhat.com/hi/nginx</code>). I tried this as they are expected to be super-small distroless images, with the goal to have zero CVEs …​ and the size of the image is really only 19MB.</p>
</div>
<div class="paragraph">
<p>The nginx that is used in this image is serving <code>dist/</code> with a serving certificate injected by OpenShift. There is no application logic in the container beyond static files.</p>
</div>
<div class="paragraph">
<p>Configuration for <strong>sensitive roles</strong> on the Cluster admins tab is delivered via <code>plugin-config.json</code> (Helm values → ConfigMap).
Default entries include <code>cluster-admin</code> and <code>admin</code>; you can add names or label ClusterRoles with <code>rbac-overview.io/elevated: &#34;true&#34;</code>.</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_prerequisites">Prerequisites</h2>
<div class="sectionbody">
<div class="ulist">
<ul>
<li>
<p>OpenShift Container Platform or OKD <strong>4.10+</strong> (dynamic console plugins)</p>
</li>
<li>
<p>For cluster install: Helm 3, <code>oc</code>, Podman or Docker, and permission to push your image registry</p>
</li>
<li>
<p>For local development: Node.js 22+, npm, and <code>oc login</code> with a <strong>bearer token</strong> (not certificate-only kubeconfig)</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>The plugin is tested against recent 4.20+ consoles. Match the console SDK generation to your cluster (the repository currently targets <code>@openshift-console/dynamic-plugin-sdk@4.21-latest</code>).</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_install_on_a_cluster">Install on a cluster</h2>
<div class="sectionbody">
<div class="sect2">
<h3 id="_install_with_helm">Install with Helm</h3>
<div class="paragraph">
<p>Log in to the cluster, then install the chart. The published and public plugin image is <code>quay.io/tjungbau/openshift-console-rbac-overview:1.0.5</code> (chart version <strong>1.0.5</strong>).</p>
</div>
<div class="paragraph">
<p>From the <a href="https://github.com/tjungbauer/openshift-console-rbac-overview" rel="noopener" target="_blank">repository</a>:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">oc login https://&lt;api-url&gt;:6443 -u &lt;user&gt; -p &#39;&lt;password&gt;&#39;

git clone https://github.com/tjungbauer/openshift-console-rbac-overview.git
cd openshift-console-rbac-overview

helm upgrade --install rbac-overview ./chart/rbac-overview \
  --namespace rbac-overview \
  --create-namespace \
  --set namespace.create=false \
  --set image.tag=1.0.5 \
  --set image.pullPolicy=Always \
  --wait</code></pre>
</div>
</div>
<div class="paragraph">
<p>Or add the published Helm repository (no clone required):</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">helm repo add rbac-overview https://tjungbauer.github.io/openshift-console-rbac-overview
helm repo update

helm upgrade --install rbac-overview rbac-overview/rbac-overview \
  --namespace rbac-overview \
  --create-namespace \
  --set namespace.create=false \
  --set image.tag=1.0.5 \
  --wait</code></pre>
</div>
</div>
<div class="paragraph">
<p>The chart defaults to <code>quay.io/tjungbau/openshift-console-rbac-overview</code>; set <code>image.tag</code> only when you need a specific release.</p>
</div>
<div class="admonitionblock warning">
<table>
<tbody><tr>
<td class="icon">
<i class="fa icon-warning" title="Warning"></i>
</td>
<td class="content">
Do not combine <code>helm --create-namespace</code> with <code>--set namespace.create=true</code>. Helm 4 reports an ownership error on the Namespace object. Use <code>--create-namespace</code> and <code>namespace.create=false</code> as shown above. I will clean this up in the future.
</td>
</tr>
</tbody></table>
</div>
</div>
<div class="sect2">
<h3 id="_build_your_own_image">Build your own image</h3>
<div class="paragraph">
<p>If you prefer to create your own image, for example in an air-gapped environment, you are free to do so. The runtime image is a thin nginx layer on top of webpack output — there is no <code>npm</code> step inside the container. This helps keep the image small and avoids unnecessary dependencies.</p>
</div>
<div class="paragraph">
<p>From the repository root:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">npm install
npm run build    # writes dist/ (plugin-manifest.json, bundles, locales)

podman build --platform=linux/amd64 \ <i class="conum" data-value="1"></i><b>(1)</b>
  -t &lt;your-registry-repository&gt;/:&lt;tag&gt; .

podman login &lt;your-registry-repository&gt;    # if your repository is private

podman push &lt;your-registry-repository&gt;/:&lt;tag&gt;</code></pre>
</div>
</div>
<div class="colist arabic">
<table>
<tbody><tr>
<td><i class="conum" data-value="1"></i><b>1</b></td>
<td>Use your own registry, repository and version tag.</td>
</tr>
</tbody></table>
</div>
<div class="paragraph">
<p>Then point Helm at your image:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">helm upgrade --install rbac-overview ./chart/rbac-overview \
  --namespace rbac-overview \
  --create-namespace \
  --set namespace.create=false \
  --set image.repository=&lt;your-registry-repository&gt; \ <i class="conum" data-value="1"></i><b>(1)</b>
  --set image.tag=&lt;tag&gt; \ <i class="conum" data-value="2"></i><b>(2)</b>
  --set image.pullPolicy=Always \
  --wait</code></pre>
</div>
</div>
<div class="colist arabic">
<table>
<tbody><tr>
<td><i class="conum" data-value="1"></i><b>1</b></td>
<td>Use your own registry and repository.</td>
</tr>
<tr>
<td><i class="conum" data-value="2"></i><b>2</b></td>
<td>Use your own version tag.</td>
</tr>
</tbody></table>
</div>
<div class="paragraph">
<p>The repository also ships <code>scripts/build-install.sh</code>, which runs <code>npm run build</code>, builds and pushes the image, installs the chart, and runs verification checks in one pass:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">chmod +x scripts/build-install.sh
QUAY_USER=&lt;your-quay-user&gt; ./scripts/build-install.sh</code></pre>
</div>
</div>
<div class="paragraph">
<p>Override <code>VERSION</code> or <code>PLATFORM</code> when the tag should differ from <code>package.json</code> or when you build on Apple Silicon for a <code>linux/amd64</code> cluster.</p>
</div>
<div class="admonitionblock note">
<table>
<tbody><tr>
<td class="icon">
<i class="fa icon-note" title="Note"></i>
</td>
<td class="content">
The script was created due to my own lazyness and it has quay.io hardcoded.
</td>
</tr>
</tbody></table>
</div>
<div class="paragraph">
<p>After install, confirm the plugin is enabled:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">oc get console.operator cluster -o jsonpath=&#39;{.spec.plugins}{&#34;\n&#34;}&#39;
oc -n rbac-overview get deploy,consoleplugin</code></pre>
</div>
</div>
<div class="paragraph">
<p>Open the console, switch to <strong>Administrator</strong>, and reload <strong>User Management → RBAC Overview</strong>.</p>
</div>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_local_development">Local development</h2>
<div class="sectionbody">
<div class="paragraph">
<p>If you like to test the plugin locally first, you can do so by running the following commands - no image build required:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash"># terminal 1
npm install
npm run start

# terminal 2
oc login ...           # must provide a token: oc whoami --show-token
npm run start-console  # console on http://localhost:9000</code></pre>
</div>
</div>
<div class="paragraph">
<p>Open <a href="http://localhost:9000/rbac-overview" rel="noopener" target="_blank">http://localhost:9000/rbac-overview</a>. The console loads the plugin from <code>localhost:9001</code> while API calls use your <code>oc</code> session.</p>
</div>
<div class="admonitionblock note">
<table>
<tbody><tr>
<td class="icon">
<i class="fa icon-note" title="Note"></i>
</td>
<td class="content">
On Apple Silicon, set <code>CONSOLE_IMAGE_PLATFORM=linux/amd64</code> before <code>npm run start-console</code>.
</td>
</tr>
</tbody></table>
</div>
<hr/>
</div>
</div>
<div class="sect1">
<h2 id="_using_the_plugin">Using the plugin</h2>
<div class="sectionbody">
<div class="sect2">
<h3 id="_who_can">Who can</h3>
<div class="paragraph">
<p>The default tab mirrors <code>oc adm policy who-can</code> for namespaced and cluster-scoped resources.</p>
</div>
<div class="paragraph">
<p><strong>Query mode</strong> offers:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><strong>Who can</strong> — list subjects allowed to perform the action</p>
</li>
<li>
<p><strong>Can subject</strong> — check one user, group, or service account</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>Set <strong>Scope</strong>, <strong>Verb</strong>, <strong>Resource</strong>, optional <strong>API group</strong>, <strong>Subresource</strong>, <strong>Resource name</strong>, and for namespaced queries a real <strong>Namespace</strong>.</p>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-who-can-results.png?width=900px" alt="Who can results for get pods in default"/>
</div>
</div>
<div class="paragraph">
<p>The button <strong>Show my access</strong> lists groups, visible namespaces, and role bindings for the logged-in user. This becomes handy when someone asks <strong>what can I actually see on this cluster?</strong></p>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-my-access.png?width=900px" alt="Show my access panel"/>
</div>
</div>
<div class="paragraph">
<p>From <strong>My access</strong> you can jump to <strong>Can subject</strong> with your user pre-filled, or open the <strong>Subjects</strong> tab on yourself.</p>
</div>
</div>
<div class="sect2">
<h3 id="_cluster_admins">Cluster admins</h3>
<div class="paragraph">
<p>This tab highlights bindings to roles you treat as <strong>elevated</strong> (configured in plugin-config.json or via the rbac-overview.io/elevated label on ClusterRoles).</p>
</div>
<div class="paragraph">
<p>Sub tabs:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><strong>Cluster-wide admins</strong> — ClusterRoleBindings to sensitive ClusterRoles.</p>
</li>
<li>
<p><strong>Namespace elevated</strong> — RoleBindings in any namespace that reference sensitive roles.</p>
</li>
</ul>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-cluster-admins.png?width=900px" alt="Cluster-wide sensitive bindings"/>
</div>
</div>
<div class="paragraph">
<p>Use <strong>View rules</strong> on a row to expand policy rules below the table.</p>
</div>
</div>
<div class="sect2">
<h3 id="_namespace_access">Namespace access</h3>
<div class="paragraph">
<p>Select a namespace, then browse:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><strong>In namespace</strong> — RoleBindings defined in that namespace.</p>
</li>
<li>
<p><strong>Cluster-wide</strong> — ClusterRoleBindings whose ClusterRoles apply there.</p>
</li>
</ul>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-namespace-access.png?width=900px" alt="Namespace access for openshift-gitops"/>
</div>
</div>
<div class="paragraph">
<p>The default filter shows <strong>User</strong> and <strong>Group</strong> subjects; add <strong>ServiceAccount</strong> when you care about workload identities.</p>
</div>
</div>
<div class="sect2">
<h3 id="_subjects">Subjects</h3>
<div class="paragraph">
<p>If you are interested to query a specific subject (user, group etc.) you can use this tab. The list merges:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>subjects found in RoleBindings and ClusterRoleBindings across the cluster, and</p>
</li>
<li>
<p>Users, Groups, and ServiceAccounts returned by the OpenShift user API (where your account may list them).</p>
</li>
</ul>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-subjects.png?width=900px" alt="Subjects tab with user and group filter"/>
</div>
</div>
<div class="paragraph">
<p>Select an entry to see binding details on the right, open linked resources, or continue in <strong>Who can → Can subject</strong>.</p>
</div>
<div class="admonitionblock note">
<table>
<tbody><tr>
<td class="icon">
<i class="fa icon-note" title="Note"></i>
</td>
<td class="content">
The error message that appears in the screenshot for the selected user is due to the fact that there are no bindings for this user. This is normal and expected.
</td>
</tr>
</tbody></table>
</div>
</div>
<div class="sect2">
<h3 id="_role_access">Role access</h3>
<div class="paragraph">
<p>The <strong>Subjects</strong> tab answers <strong>who has what bound to them?</strong> <strong>Role access</strong> flips the question: <strong>who is bound to this ClusterRole?</strong> That is useful when you audit a built-in or custom ClusterRole (<code>view</code>, <code>edit</code>, <code>cluster-admin</code>, or a role you ship with an operator) and need every ClusterRoleBinding and RoleBinding that references it in one table.</p>
</div>
<div class="paragraph">
<p>Pick a <strong>ClusterRole</strong> from the dropdown. The table lists matching bindings with scope, namespace (for namespaced RoleBindings), role kind, subjects, and <strong>View rules</strong> to expand the ClusterRole policy inline.</p>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-role-access.png?width=900px" alt="Role access for ClusterRole cluster-admin"/>
</div>
</div>
<div class="paragraph">
<p>The screenshot shows <code>cluster-admin</code> with 30 bindings — a mix of human users, the <code>cluster-admins</code> group, and OpenShift system groups such as <code>system:openshift:operator:kube-apiserver-operator</code>.</p>
</div>
</div>
<div class="sect2">
<h3 id="_scc_access">SCC access</h3>
<div class="paragraph">
<p>To get a quick overview about SCC and who is on which SCC was always tricky.</p>
</div>
<div class="paragraph">
<p>OpenShift SCCs authorise subjects in two ways:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>direct fields on the SCC object</p>
</li>
<li>
<p>RBAC to <code>use securitycontextconstraints</code>.</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>This tab shows every SCC and their configuration.</p>
</div>
<div class="imageblock">
<div class="content">
<img src="/openshift-platform/security/RBAC/images/rbac-overview-scc-access.png?width=900px" alt="All security context constraints"/>
</div>
</div>
<div class="ulist">
<ul>
<li>
<p><strong>All SCCs</strong> — every SCC you can list, with priority, privileged/host settings, run-as-user and SELinux strategy, volumes, and direct subject count.</p>
</li>
<li>
<p><strong>Direct authorizations</strong> — users, groups, and service accounts listed on each SCC.</p>
</li>
<li>
<p><strong>RBAC use grants</strong> — ClusterRoleBindings that grant <code>use</code> on SCC resources.</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>This tab does not replace a full SCC compliance review, but it saves time when you are tracing why a pod was allowed a privileged context.</p>
</div>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_limitations">Limitations</h2>
<div class="sectionbody">
<div class="ulist">
<ul>
<li>
<p><strong>Who can</strong> requires the same elevated create permissions as <code>oc adm policy who-can</code>.</p>
</li>
<li>
<p>Large clusters: tables paginate, but initial binding scans can take a moment on first tab open.</p>
</li>
</ul>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_summary">Summary</h2>
<div class="sectionbody">
<div class="paragraph">
<p>RBAC Overview does not replace <code>oc</code> or your GitOps RBAC manifests. It gives operators and auditors a single console entry point for the questions that come up every week — who is admin, what does this namespace grant, what is bound to this service account, who can touch this verb/resource, and which identities sit on sensitive SCCs.</p>
</div>
<div class="paragraph">
<p>The plugin was created with the help of AI (I always want to be open about that), especially the initial layout, some styling elements like dropdowns and buttons and the README …​ which I never want to write.</p>
</div>
<div class="paragraph">
<p>If you try it, feedback and issues are welcome on the <a href="https://github.com/tjungbauer/openshift-console-rbac-overview" rel="noopener" target="_blank">repository</a>.</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_references">References</h2>
<div class="sectionbody">
<div class="ulist">
<ul>
<li>
<p><a href="https://github.com/tjungbauer/openshift-console-rbac-overview" rel="noopener" target="_blank">openshift-console-rbac-overview on GitHub</a></p>
</li>
<li>
<p><a href="https://quay.io/repository/tjungbau/openshift-console-rbac-overview?tab=tags" rel="noopener" target="_blank">openshift-console-rbac-overview on Quay</a></p>
</li>
<li>
<p><a href="https://github.com/openshift/console/tree/main/frontend/packages/console-dynamic-plugin-sdk" rel="noopener" target="_blank">OpenShift Console dynamic plugin SDK</a></p>
</li>
<li>
<p><a href="https://docs.openshift.com/container-platform/latest/web_console/dynamic-plugins-overview.html" rel="noopener" target="_blank">OpenShift dynamic plugins documentation</a></p>
</li>
<li>
<p><a href="https://hummingbird-project.io/" rel="noopener" target="_blank">Project Hummingbird</a></p>
</li>
</ul>
</div>
</div>
</div>]]></content:encoded></item><item><title>OKD Single node installation in a disconnected environment</title><link>https://blog.stderr.at/openshift-platform/infrastructure/2026-02-09-okd-sno-disconnected/</link><guid isPermaLink="true">https://blog.stderr.at/openshift-platform/infrastructure/2026-02-09-okd-sno-disconnected/</guid><pubDate>Mon, 09 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Articles by Toni Schmidbauer</dc:creator><category>OKD</category><category>Single Node</category><category>Disconnected</category><category>Installation</category><description>OKD Single node installation in a disconnected environment</description><content:encoded><![CDATA[<div class="paragraph">
<p>Because of reasons we had to set up a disconnected single node OKD &#34;cluster&#34;. This is our brain dump as the
OKD documentation sometimes refers to OpenShift image locations, we had to read multiple sections to get it working and we
also consulted the OpenShift documentation at <a class="bare" href="https://docs.redhat.com">https://docs.redhat.com</a>.</p>
</div>
<div class="admonitionblock note">
<table>
<tbody><tr>
<td class="icon">
<i class="fa icon-note" title="Note"></i>
</td>
<td class="content">
Updated on 2026-03-03: Fixed install-config.yaml and create manifest command
</td>
</tr>
</tbody></table>
</div>
<div class="paragraph">
<p>First we need to download the <em>oc</em> command in the version we would like to install. OKD provides it on its release page:</p>
</div>
<div class="paragraph">
<p><a class="bare" href="https://github.com/okd-project/okd/releases/download/4.21.0-okd-scos.3/openshift-client-linux-amd64-rhel9-4.21.0-okd-scos.3.tar.gz">https://github.com/okd-project/okd/releases/download/4.21.0-okd-scos.3/openshift-client-linux-amd64-rhel9-4.21.0-okd-scos.3.tar.gz</a></p>
</div>
<div class="paragraph">
<p>To get an overview of releases available for OKD see <a class="bare" href="https://amd64.origin.releases.ci.openshift.org/">https://amd64.origin.releases.ci.openshift.org/</a>.</p>
</div>
<div class="paragraph">
<p>Another option is to extract the <em>oc</em> command from the release image, if you have a version of <em>oc</em> already installed:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">oc adm release extract --tools quay.io/okd/scos-release:4.21.0-okd-scos.3</code></pre>
</div>
</div>
<div class="paragraph">
<p>This will also extract the <em>openshift-install</em> tar.gz which we need later to generate the installation manifests and ignition configs.</p>
</div>
<div class="paragraph">
<p>Next we need the <em>oc-mirror</em> plugin, which is used to mirror the release content to our local registry. We were only able to find the plugin on <em>console.redhat.com</em>. Another option might be to compile the plugin from source code (<a class="bare" href="https://github.com/openshift/oc-mirror/">https://github.com/openshift/oc-mirror/</a>).</p>
</div>
<div class="paragraph">
<p>We copied the <em>oc</em> command and the <em>oc-mirror</em> plugin to /usr/local/bin/ and made them executable. After that we ran <em>oc mirror --v2 --help</em> to test the installation.</p>
</div>
<div class="paragraph">
<p>The upstream <a href="https://github.com/openshift/oc-mirror/blob/main/docs/okd-mirror.md">oc-mirror</a> plugin documentation was also helpful.</p>
</div>
<div class="paragraph">
<p>As our private registry required authentication, we created a .dockerconfigjson file with the registry credentials:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-json hljs" data-lang="json">{
  &#34;auths&#34;: {
    &#34;internal.registry&#34;: {
      &#34;auth&#34;: &#34;&lt;credentials base64 encoded&gt;&#34;,
      &#34;email&#34;: &#34;you@example.com&#34;
    }
  }
}</code></pre>
</div>
</div>
<div class="paragraph">
<p>You can encode the credentials with:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">echo -n &#34;username:password&#34; | base64 -w0</code></pre>
</div>
</div>
<div class="paragraph">
<p>For mirroring all required OKD images to our private registry we created an <em>ImageSetConfiguration</em>:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-yaml hljs" data-lang="yaml">kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  platform:
    channels:
    - name: 4-scos-stable
      minVersion: 4.21.0-okd-scos.3
      maxVersion: 4.21.0-okd-scos.3
    graph: false
  operators:
    - registry: quay.io/okderators/catalog-index:testing-4.20 <i class="conum" data-value="1"></i><b>(1)</b>
      packages:
       - name: aws-load-balancer-operator
       - name: 3scale-operator
       - name: node-observability-operator
  additionalImages:
   - name: registry.redhat.io/ubi8/ubi:latest
   - name: registry.redhat.io/ubi9/ubi@sha256:20f695d2a91352d4eaa25107535126727b5945bff38ed36a3e59590f495046f0</code></pre>
</div>
</div>
<div class="paragraph">
<p>We used a minimal configuration without operators and additional images, because downloading additional images did not work for us:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-yaml hljs" data-lang="yaml">kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  platform:
    channels:
    - name: 4-scos-stable <i class="conum" data-value="1"></i><b>(1)</b>
      type: okd
      minVersion: 4.21.0-okd-scos.3 <i class="conum" data-value="2"></i><b>(2)</b>
      maxVersion: 4.21.0-okd-scos.3
    graph: false
  operators: []
  additionalImages: []</code></pre>
</div>
</div>
<div class="paragraph">
<p>The hard part for us was finding the right values for <em>channels</em> and
the min/max versions. Once again the OKD
<a href="https://amd64.origin.releases.ci.openshift.org/">release status page</a>
was helpful.</p>
</div>
<div class="paragraph">
<p>Our private registry is based on <a href="https://goharbor.io">Harbor</a> for our
experiments, but any Docker v2 compatible registry should work. The
only thing required is a project within Harbor called <em>openshift</em> and
a user/password for pulling/pushing images to this project. The
project name might be configurable with <em>oc mirror</em>, but this requires
further investigation.</p>
</div>
<div class="paragraph">
<p><em>oc mirror</em> wants to verify signatures so we had to download a public
key and provide the <em>OCP_SIGNATURE_URL</em>:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">curl -LO https://raw.githubusercontent.com/openshift/cluster-update-keys/master/keys/verifier-public-key-openshift-ci-4
export OCP_SIGNATURE_URL=&#34;https://storage.googleapis.com/openshift-ci-release/releases/signatures/openshift/release/&#34;
export OCP_SIGNATURE_VERIFICATION_PK=&#34;verifier-public-key-openshift-ci-4&#34;

# for debugging add --log-level debug, but we had intermittent errors with this option.
# after removing --log-level debug oc mirror ran without problems
oc mirror -c ImageSetConfiguration.yaml --authfile docker-auth.json --workspace file:///workspace docker://internal.registry --v2</code></pre>
</div>
</div>
<div class="paragraph">
<p>This will create the <em>ImageDigestMirrorSource</em> in <em>/workspace/working-dir/cluster-resources/idms-oc-mirror.yaml</em> and <em>ImageTagMirrorSource</em> custom resources in <em>/workspace/working-dir/cluster-resources/itms-oc-mirror.yaml</em>.</p>
</div>
<div class="paragraph">
<p>The content of <em>ImageDigestMirrorSource</em> will be reused in
the <em>install-config.yaml</em>. This is required to redirect image pulls done by the node from <em>quay.io</em> to our private registry.</p>
</div>
<div class="paragraph">
<p>Next we need DNS records for our cluster:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>api.sno.internal</p>
</li>
<li>
<p>api-int.sno.internal</p>
</li>
<li>
<p>*.apps.sno.internal (wildcard DNS entry for applications deployed on the cluster)</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>Now we prepared the required <em>install-config.yaml</em> file to trigger the <em>openshift-install</em> command.
We basically followed <a href="https://docs.okd.io/latest/installing/installing_sno/install-sno-installing-sno.html">Installing a Single Node OpenShift Cluster</a>.</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-yaml hljs" data-lang="yaml">apiVersion: v1
baseDomain: internal
compute:
- hyperthreading: Enabled
  name: worker
  replicas: 0 <i class="conum" data-value="1"></i><b>(1)</b>
controlPlane:
  hyperthreading: Enabled
  name: master
  replicas: 1 <i class="conum" data-value="2"></i><b>(2)</b>
metadata:
  name: okd-sno
networking:
  clusterNetwork:
  - cidr: 10.128.0.0/14
    hostPrefix: 23
  networkType: OVNKubernetes
  serviceNetwork:
  - 172.30.0.0/16
platform:
  none: {}
fips: false
sshKey: &#39;the key&#39;
bootstrapInPlace: <i class="conum" data-value="3"></i><b>(3)</b>
  installationDisk: /dev/vda
pullSecret: |
  {
    &#34;auths&#34;: {
      &#34;registry.internal&#34;: {
        &#34;auth&#34;: &#34;&lt;username:password base64 encoded&gt;&#34;,
        &#34;email&#34;: &#34;some@email&#34;
      }
    }
  }
additionalTrustBundle: | <i class="conum" data-value="4"></i><b>(4)</b>
  -----BEGIN CERTIFICATE-----
  ZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZZ
  -----END CERTIFICATE-----
ImageDigestSources: <i class="conum" data-value="5"></i><b>(5)</b>
- mirrors:
  - registry.internal/openshift/release
  source: quay.io/okd/scos-content
- mirrors:
  - registry.internal/openshift/release-images
  source: quay.io/okd/scos-release</code></pre>
</div>
</div>
<div class="colist arabic">
<table>
<tbody><tr>
<td><i class="conum" data-value="1"></i><b>1</b></td>
<td>We set the number of workers to zero (0). Because this is a SNO installation. Worker nodes could be added later, even for a SNO cluster</td>
</tr>
<tr>
<td><i class="conum" data-value="2"></i><b>2</b></td>
<td>We set the number of master nodes to one (1)</td>
</tr>
<tr>
<td><i class="conum" data-value="3"></i><b>3</b></td>
<td>This tells the installer that we do not have a bootstrap node and it should create one large ignition config file</td>
</tr>
<tr>
<td><i class="conum" data-value="4"></i><b>4</b></td>
<td>If required the CA certificate of our registry. This is required if the registry uses an internal certificate</td>
</tr>
<tr>
<td><i class="conum" data-value="5"></i><b>5</b></td>
<td>ImageDigestSources configures CRIO to redirect requests to quay.io to our private registry.</td>
</tr>
</tbody></table>
</div>
<div class="paragraph">
<p>It’s time to create the installation manifests and ignition configs. We created a directory <em>install</em>, copied the <em>install-config.yaml</em> into this directory and triggered the <em>openshift-install</em> command:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">$ mkdir install
$ cp install-config.yaml install/
$ openshift-install --dir=install create single-node-ignition-config</code></pre>
</div>
</div>
<div class="paragraph">
<p>We are now ready to download the installation ISO and to embed our ignition config into it. We also want
to set kernel boot arguments to configure the network interface with a static IP address.</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">curl -L $( ./openshift-install coreos print-stream-json |jq -r &#34;.architectures.x86_64.artifacts.metal.formats.iso.disk.location&#34; ) -o fhcos-live.iso</code></pre>
</div>
</div>
<div class="paragraph">
<p>The next step is to modify the ISO. We basically followed the instructions in the OKD documentation:
<a class="bare" href="https://docs.okd.io/4.14/installing/installing_sno/install-sno-installing-sno.html#generating-the-install-iso-manually_install-sno-installing-sno-with-the-assisted-installer">https://docs.okd.io/4.14/installing/installing_sno/install-sno-installing-sno.html#generating-the-install-iso-manually_install-sno-installing-sno-with-the-assisted-installer</a></p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-shell hljs" data-lang="shell">alias coreos-installer=&#39;podman run --privileged --pull always --rm -v /dev:/dev -v /run/udev:/run/udev -v $PWD:/data -w /data quay.io/coreos/coreos-installer:release&#39;
coreos-installer iso ignition embed -fi install/bootstrap-in-place-for-live-iso.ign fcos-live.iso
coreos-installer iso kargs modify --append &#39;ip=10.0.0.99::10.0.0.1:255.255.255.0:sno.internal:ens33:none:10.0.0.255&#39; fcos-live.iso</code></pre>
</div>
</div>
<div class="paragraph">
<p>We can take a look at the ISO bootstrap config and the kernel arguments with:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlightjs highlight"><code class="language-bash hljs" data-lang="bash">coreos-installer iso ignition show fcos-live.iso
coreos-installer iso kargs show fcos-live.iso</code></pre>
</div>
</div>
<div class="paragraph">
<p>For understanding the kernel boot arguments see <a class="bare" href="https://access.redhat.com/solutions/5499911">https://access.redhat.com/solutions/5499911</a>.</p>
</div>
<div class="paragraph">
<p>The final step is to boot the modified ISO on the target machine and wait for the installation to complete.</p>
</div>]]></content:encoded></item></channel></rss>